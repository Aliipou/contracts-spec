{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://contracts-spec/decision-os/decision.schema.json",
  "title": "Decision",
  "description": "The SINGLE decision contract — the OUTPUT of the kernel. Exactly one repo (decision-kernel-core) may emit an instance of this. control-plane, fdk-research, and agent-runtime MUST NOT. This is the most safety-critical schema in the system.",
  "type": "object",
  "additionalProperties": false,
  "required": ["verdict", "reason", "action_ref", "issued_by"],
  "properties": {
    "verdict": {
      "type": "string",
      "enum": ["ALLOW", "DENY", "LIMIT", "DEFER"],
      "description": "ALLOW = permit as submitted. DENY = refuse. LIMIT = permit only a constrained/minimized form (see obligations/transformed_payload); this is the canonical name for the AuthGate policy engine's TRANSFORM. DEFER = cannot decide now; escalate."
    },
    "reason": {
      "type": "string",
      "minLength": 1,
      "description": "Deterministic, human-readable justification. No probability, no ML narrative."
    },
    "action_ref": {
      "type": "string",
      "minLength": 1,
      "description": "Binds this decision to the exact action it rules on (the action's nonce or a content hash). A decision is only valid for the action_ref it names."
    },
    "issued_by": {
      "type": "string",
      "minLength": 1,
      "description": "Identity of the AUTHORITY that produced this decision. MUST be the decision-kernel-core. CI/runtime rejects a decision whose issued_by is not the kernel."
    },
    "obligations": {
      "type": "array",
      "items": { "type": "string" },
      "default": [],
      "description": "For LIMIT: the constraints applied (e.g. 'redacted:ssn', 'redacted:content:pattern'). Empty for ALLOW/DENY/DEFER."
    },
    "transformed_payload": {
      "type": ["object", "null"],
      "default": null,
      "description": "For LIMIT only: the minimized payload the executor must use instead of the original. Null otherwise."
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "When the decision was issued (RFC3339/ISO-8601 UTC)."
    }
  },
  "allOf": [
    {
      "if": { "properties": { "verdict": { "const": "LIMIT" } } },
      "then": { "required": ["obligations"] }
    }
  ]
}
